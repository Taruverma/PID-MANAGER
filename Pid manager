//Following are the header files I'm going to use 
#include<stdio.h>
#include<unistd.h>
#include<stdlib.h>
#include<sys/wait.h>
#include<pthread.h>

//Following are the default values given to the project
#define MAX_PID 1000
#define MIN_PID 100

//I'm going to use the Following functions in my Project 
int allocate map(void);

int allocate_pid(void);

void release_pid(int id);


int main();
